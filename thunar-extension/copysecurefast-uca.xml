<?xml version="1.0" encoding="UTF-8"?>
<!--
  CopySecureFast - Custom Actions for Thunar (TeraCopy/SuperCopier flow).
  To install: cp thunar-extension/copysecurefast-uca.xml ~/.config/Thunar/uca.xml
  (or merge the <action> elements into your existing uca.xml).

  Then restart Thunar (close all windows) and reopen.

  Usage flow:
  1. Select files/folders, right-click -> "Copiar con CopySecureFast"
     -> right-click on destination folder -> "Pegar con CopySecureFast"
  2. The floating window appears automatically with the live queue.
-->
<actions>

  <!-- Previous user action: open terminal (preserved) -->
  <action>
    <icon>utilities-terminal</icon>
    <name>Open Terminal Here</name>
    <unique-id>1761235802726116-1</unique-id>
    <command>kitty</command>
    <description>Open kitty terminal in this directory</description>
    <patterns>*</patterns>
    <directories/>
  </action>

  <!--
    Copiar con CSF - marks files in the CSF clipboard. Does NOT copy
    yet. The actual copy happens when the user right-clicks on the
    destination folder -> "Pegar con CopySecureFast". This is the
    TeraCopy/SuperCopier flow.
  -->
  <action>
    <icon>edit-copy</icon>
    <name>Copiar con CopySecureFast</name>
    <unique-id>csf-copy</unique-id>
    <description>Mark files for copying (then 'Pegar con CSF' on destination)</description>
    <command>sh -c 'csf-thunar-helper copy "$@" &amp;&amp; notify-send "CopySecureFast" "Marcados para copiar. Click derecho en destino → Pegar con CopySecureFast" || true' sh</command>
    <patterns>*</patterns>
    <directories/>
    <other-files/>
    <text-files/>
    <image-files/>
    <audio-files/>
    <video-files/>
  </action>

  <action>
    <icon>edit-cut</icon>
    <name>Cortar con CopySecureFast</name>
    <unique-id>csf-cut</unique-id>
    <description>Mark files for moving (then 'Pegar con CSF' on destination)</description>
    <command>sh -c 'csf-thunar-helper cut "$@" &amp;&amp; notify-send "CopySecureFast" "Marcados para mover. Click derecho en destino → Pegar con CopySecureFast" || true' sh</command>
    <patterns>*</patterns>
    <directories/>
    <other-files/>
    <text-files/>
    <image-files/>
    <audio-files/>
    <video-files/>
  </action>

  <!-- Pegar con CSF: enqueues the clipboard items in this folder.
       Uses %d (current directory in Thunar) so it works both for
       right-click on the background and on a folder item. -->
  <action>
    <icon>edit-paste</icon>
    <name>Pegar con CopySecureFast</name>
    <unique-id>csf-paste</unique-id>
    <description>Enqueue copy/move of the marked items in this folder</description>
    <command>sh -c 'csf-thunar-helper paste "$1" || notify-send "CopySecureFast" "Clipboard CSF vacío"' sh %d</command>
    <patterns>*</patterns>
    <directories/>
  </action>

  <!--
    Copy to...: right-click on files -> opens a native folder picker.
  -->
  <action>
    <icon>folder-copy</icon>
    <name>Copiar a...</name>
    <unique-id>csf-copy-to</unique-id>
    <description>Copy selected items to a chosen folder</description>
    <command>sh -c 'DEST=$(zenity --file-selection --directory --title="Copiar con CopySecureFast a..." 2&gt;/dev/null) &amp;&amp; [ -n "$DEST" ] &amp;&amp; csf-thunar-helper copy "$@" &amp;&amp; csf-thunar-helper paste "$DEST" || true' sh %F</command>
    <patterns>*</patterns>
    <directories/>
    <other-files/>
    <text-files/>
    <image-files/>
    <audio-files/>
    <video-files/>
  </action>

  <action>
    <icon>folder-move</icon>
    <name>Mover a...</name>
    <unique-id>csf-move-to</unique-id>
    <description>Move selected items to a chosen folder</description>
    <command>sh -c 'DEST=$(zenity --file-selection --directory --title="Mover con CopySecureFast a..." 2&gt;/dev/null) &amp;&amp; [ -n "$DEST" ] &amp;&amp; csf-thunar-helper cut "$@" &amp;&amp; csf-thunar-helper paste "$DEST" || true' sh %F</command>
    <patterns>*</patterns>
    <directories/>
    <other-files/>
    <text-files/>
    <image-files/>
    <audio-files/>
    <video-files/>
  </action>

  <!-- Drop: drop files onto a folder, copy/move -->
  <action>
    <icon>edit-copy</icon>
    <name>Copiar aquí con CopySecureFast</name>
    <unique-id>csf-drop-copy</unique-id>
    <description>Drop: copy selected items into this folder</description>
    <command>sh -c 'csf-thunar-helper copy "$@" &amp;&amp; csf-thunar-helper paste "$1" || true' sh %F %f</command>
    <patterns>*</patterns>
    <directories/>
    <other-files/>
    <text-files/>
    <image-files/>
    <audio-files/>
    <video-files/>
  </action>

  <action>
    <icon>edit-cut</icon>
    <name>Mover aquí con CopySecureFast</name>
    <unique-id>csf-drop-move</unique-id>
    <description>Drop: move selected items into this folder</description>
    <command>sh -c 'csf-thunar-helper cut "$@" &amp;&amp; csf-thunar-helper paste "$1" || true' sh %F %f</command>
    <patterns>*</patterns>
    <directories/>
    <other-files/>
    <text-files/>
    <image-files/>
    <audio-files/>
    <video-files/>
  </action>

  <!-- Utilities -->
  <action>
    <icon>view-list-symbolic</icon>
    <name>Ver cola de CopySecureFast</name>
    <unique-id>csf-queue</unique-id>
    <description>Open the floating queue window</description>
    <command>csf-thunar-helper queue</command>
    <patterns>*</patterns>
    <directories/>
    <other-files/>
    <text-files/>
    <image-files/>
    <audio-files/>
    <video-files/>
  </action>

  <action>
    <icon>preferences-system-network</icon>
    <name>CSF: estado del daemon</name>
    <unique-id>csf-status</unique-id>
    <description>Show the queue and daemon state as a notification</description>
    <command>sh -c 'csf-thunar-helper status | xargs -I{} notify-send "CopySecureFast" "{}" || true'</command>
    <patterns>*</patterns>
    <directories/>
  </action>
</actions>
